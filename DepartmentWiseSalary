--A company's executives are interested in seeing who earns the most money in each of the company's departments. A high earner in a department is an employee who has a salary in the top three unique salaries for that department.

--Write an SQL query to find the employees who are high earners in each of the departments.





CREATE TABLE #CLINICAL(ID INT,NAME VARCHAR(20),SALARY BIGINT,DEPTID INT)


INSERT INTO #CLINICAL VALUES
(1,   'Joe',    85000,   1),            
(2,   'Henry',  80000,   2),            
(3,   'Sam',    60000,   2),           
(4,   'Max',    90000,   1),            
(5,   'Janet',  69000,   1) ,           
(6 ,  'Randy',  85000,   1) ,          
(7,   'Will',   70000,   1)   







CREATE TABLE #DEPT(ID INT,NAME VARCHAR(20))

INSERT INTO #DEPT VALUES
(1  , 'IT'),
(2  , 'Sales' )

SELECT DNAME DEPARTMENT,NAME Employee,salary from(

SELECT C.NAME,C.SALARY,D.NAME DNAME,
DENSE_RANK() OVER(PARTITION BY DEPTID ORDER BY C.SALARY DESC) RNK
FROM 
#CLINICAL C
JOIN #DEPT D
ON C.DEPTID=D.ID)pvt
WHERE RNK BETWEEN 1 AND 3
ORDER BY DEPARTMENT,SALARY DESC
